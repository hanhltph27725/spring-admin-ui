# URL gốc của backend (Spring Boot). Để trống khi chạy local (Vite proxy sẽ lo).
# Khi deploy Vercel, đặt bằng URL công khai của BE, ví dụ:
# VITE_API_BASE_URL=https://datacore-app.up.railway.app
VITE_API_BASE_URL=
